# Vertical slice: minifyEnabled is off in both build types for now, so this
# file is a placeholder for when release hardening (see docs/DISCOVERY.md
# milestone 6) turns it on. kotlinx.serialization + Room + Retrofit each ship
# their own consumer ProGuard rules, so nothing extra is required to build.
